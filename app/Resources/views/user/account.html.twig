{% extends 'base.html.twig' %}

{% block title %}
    Minha Conta{{ parent() }}
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Minha Conta</h1>

        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_widget(form) }}
            <input type="submit" value="Update">
        {{ form_end(form) }}

    </div>
{% endblock %}
